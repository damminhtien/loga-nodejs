extends layout

block content
    h1= title
    p Welcome to #{title}
    div#danhsach
        p Danh sách
    // |   <script>
    // |   $(document).ready(function() {
    //         trangchu()

    //         function trangchu() {
    //         |   $.post(
    //                 "/trangchu", {
    //                 },
    //                 function(data) {
    //                     data.forEach(function(item) {
    //                         var title = item.title;
    //                         danhsach += '<p><a  href="#"' + title + '<p></a>';
    //                     });
    //                 |   $('#danhsach').html(danhsach);
    //                 }
    //         |   );
    //         }
    // |   })
    // |   </script>
    // script (src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js")